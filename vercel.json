{
  "buildCommand": "pip install -r requirements.txt && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "outputDirectory": ".next",
  "functions": {
    "api/index.py": {
      "runtime": "python3.11"
    }
  }
}